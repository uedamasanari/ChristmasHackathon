クリスマスハッカソン用のリポジトリです

作品概要
・アカウント作成：メルアド、サンタ(親)側のパスワード
・ログイン：サンタ⇒ログイン画面でメルアドとパスワード　サンタが子供用アカウント作成　子ども⇒サンタが作った「名前(一意)」でログイン

新規登録
サンタログイン
    子ども管理
    プレゼント一覧
    ログアウト

子どもログイン
    プレゼント概要送信
    ログアウト

import axios from 'axios';

async function getData() {
  try {
    const response = await axios.get('https://example.com/api/endpoint');
    console.log(response.data);
  } catch (error) {
    console.error(error);
  }
}

getData();

import axios from 'axios';

async function postData() {
  try {
    const response = await axios.post('https://example.com/api/endpoint', {
      key: 'value'
    });
    console.log(response.data);
  } catch (error) {
    console.error(error);
  }
}

postData();
